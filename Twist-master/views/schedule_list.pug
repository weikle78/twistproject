extends layout

block content
  h1= title

  ul
    each school in schedule_list
      li
        a(href=schedule.url) #{schedule.title}
        |  (#{schedule.sessionId.name})

    else
      li There are no schedules.
