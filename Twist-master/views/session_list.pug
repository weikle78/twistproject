
extends layout

block content
  h1= title

  ul
    each session in session_list
      li
        a(href=session.url) #{session.SessionNum}
        |  #{session.ParticipantID}
        |  #{session.Time}
    
    
    else
      li There are no sessions.

