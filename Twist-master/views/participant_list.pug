extends layout

block content
  h1= title
  
  ul
    each participant in participant_list
      li 
        a(href=participant.url) #{participant.name} 

    else
      li There are no participants.